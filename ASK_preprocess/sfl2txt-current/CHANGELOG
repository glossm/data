v. 0.7
	* Presets can now be stored in config file
	
v. 0.6
	* New option -s for using input filename as the output, changing the extension

v. 0.5
	* New syntax and options
	* reading from/to stdin/stdout everywhere, no longer using /dev/stdin
	   and /dev/stdout, probably improving crossplatform compatibility
	* Added manpage
	
v. 0.4.1
	* Minor bugfix: precision in TXT files 

v. 0.4
	* All status messages are now written to STDERR
	* Fixed broken reading of time values from TXT
	* Added export to STDOUT and import from STDIN for better interoperability with iconv
